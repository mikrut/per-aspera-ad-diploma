<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/file_row_selector"
    xmlns:tools="http://schemas.android.com/tools">

	<ImageView
        android:id="@+id/fdrowimage"
        tools:src="@drawable/ic_folder_black_24dp"
		android:layout_alignParentLeft="true"
		android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
		android:layout_height="24dp"
        android:layout_width="24dp"
		android:layout_margin="10dp" />

	<TextView
        android:id="@+id/fdrowtext"
        tools:text="Hello!"
        android:layout_width="wrap_content"
        android:layout_toRightOf="@+id/fdrowimage"
		android:layout_alignParentTop="true"
		android:gravity="center_vertical"
        android:layout_height="wrap_content"
        android:paddingTop="5dp"
        android:fontFamily="sans-serif-medium"
        android:ellipsize="end"
        android:singleLine="true" />

    <TextView
        android:id="@+id/fdrowsubtext"
        tools:text="subtext"
        android:layout_alignStart="@id/fdrowtext"
        android:layout_alignLeft="@id/fdrowtext"
        android:layout_below="@id/fdrowtext"
        android:layout_alignParentBottom="true"
        android:textColor="@color/textColorSecondary"
        android:fontFamily="sans-serif-light"
        android:textSize="12sp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:singleLine="true"/>

</RelativeLayout>